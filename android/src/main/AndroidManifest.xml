<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="io.wazo.callkeep">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"
                     android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />

    <!-- Required (Android 11+) so resolveActivity can see the system telecom
         package when opening the "Calling Accounts" settings screen. -->
    <queries>
        <package android:name="com.android.server.telecom" />
    </queries>
</manifest>
